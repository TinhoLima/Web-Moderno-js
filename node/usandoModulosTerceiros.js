const _ = require('lodash')
setInterval(() => console.log(_.random(500, 600)), 2000) // --------> gerando números aleatorios apartir da biblioteca que instalamos 'lodash', lindo!

// Instalamos o nodemon (npm i -g nodemon)
// selecionamos "cd node" pra selecionarmos a pasta que queremos chegar
// usando nodemon e nome do que iremos executar "nodemon usandoModuloTerceiros.js" ele executa nosso código automaticamente e em tempo real.